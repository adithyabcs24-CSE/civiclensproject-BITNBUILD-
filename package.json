{
  "name": "civiclens-ai",
  "version": "1.0.0",
  "description": "CivicLens AI — Municipal Document Intelligence Platform",
  "scripts": {
    "start": "node backend/src/index.js",
    "seed": "npm --prefix backend run seed",
    "test": "npm --prefix backend test",
    "build": "npm --prefix frontend run build",
    "setup": "npm --prefix backend install && npm --prefix frontend install && npm run build && npm run seed"
  }
}
